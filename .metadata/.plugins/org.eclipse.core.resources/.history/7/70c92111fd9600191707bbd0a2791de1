1.忘记大括号 解决办法:永远在if和else后面加上大括号
2.在if后面千万别跟着分号
3.错误使用==和=   =是赋值
4.代码风格 在if和else 之后必须加上大括号，大括号内加tab
5.7>=3+4 所有关系运算符的优先级比算术运算低,但是比赋值高 判断是否相等的==和!=的优先级比其他的低
int 4个字节 byte
强烈建议：只要是for do while while语句，就一定跟上一对大括号
Tips for loops 如果有固定次数用for 如果必须执行一次 用do while 其他情况用while
		